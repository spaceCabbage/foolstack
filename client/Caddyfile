# Production Caddyfile for Vue SPA
# Handles client-side routing with try_files fallback

:{$CLIENT_PORT:80} {
    root * /app/dist

    # SPA routing - fallback to index.html for client-side routes
    try_files {path} /index.html

    file_server

    encode gzip
}
